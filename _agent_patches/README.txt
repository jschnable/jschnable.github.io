APPLY ONTO master versions of _data/news.yml and _data/publications.yml then replace those files on this branch.
Also restore _data/lab_authors.yml from master then apply lab_authors.patch.
CRITICAL: current _data/lab_authors.yml on this branch is a broken stub — do not merge until restored.
No SMART1 paper (editor only).
Local full files ready at box paths under /workspace/schnable-site/_data/ and MCP JSON /tmp/mcp-args/push_all_three.json
